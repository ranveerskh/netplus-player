NetPlus IPTV Player — Fresh Build Manifest
Version: 1.5.2
Build date: 2026-08-24
Build label: Mixed VOD Hierarchy + Live Stability + Dashboard Repair

Included fixes
1. Mixed VOD show detection using the provider's VOD hierarchy.
2. Show flow: title -> seasons -> episodes -> quality -> playback.
3. Movie flow: title -> quality -> playback.
4. Provider "nothing_to_play" errors are reported clearly instead of failing silently.
5. Live TV recovery no longer forces a fresh link every 15 seconds; soft HLS recovery runs first.
6. Non-fatal levelParsingError and mediaSourceRequiresReset recovery is handled in place.
7. Dashboard and media grids have responsive overflow/layout repairs.
8. GitHub Actions Windows NSIS installer workflow is included.
9. electron-builder publishing is explicitly disabled for CI installer builds.

Verified before packaging
- local-player/server.cjs passed node --check.
- local-player/app.js passed node --check.
- package.json version is 1.5.2.
- desktop:make is electron-builder --win nsis --publish never.
- No portal credentials or config.json are included.

Provider note
The application cannot restore a movie or episode whose upstream provider storage returns
"nothing_to_play" or is offline. Version 1.5.2 reports that condition accurately so another
title can be selected.
